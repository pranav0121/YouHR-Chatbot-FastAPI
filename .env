# PostgreSQL Database Configuration
# Example (replace user, password, host, port, database as needed):
# DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<database>
# Local example below (already filled for convenience). Update the password and port if your Postgres uses default 5432.
DATABASE_URL=postgresql://postgres:pranav@localhost:5433/you_assistant

# Notes:
# - On Windows PowerShell, to run uvicorn with this .env you can start your shell in the project folder
#   and run: setx DATABASE_URL "postgresql://postgres:your_password@localhost:5432/you_assistant" ; refresh the shell
# - Alternatively create a virtual environment and use a .env loader (python-dotenv is already used in the project).
